import { defineMessages } from 'react-intl';

const scope = 'page.home';

const homeI18n = defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Привіт!!',
  },
});

export default homeI18n;
