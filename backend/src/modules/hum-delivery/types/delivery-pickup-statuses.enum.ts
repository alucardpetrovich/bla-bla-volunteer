export enum DeliveryPickupStatuses {
  REQUESTED = 'requested',
  DRIVER_ACCEPTED = 'driver_accepted',
  HUB_ACCEPTED = 'hub_accepted',
  DRIVER_REJECTED = 'driver_rejected',
  SCHEDULED = 'scheduled',
  PICKED_UP = 'picked_up',
  DELIVERED = 'delivered',
  FRAUDULENT = 'fraudulent',
}
