export enum DeliveryStatuses {
  REQUESTED = 'requested',
  DRIVER_ACCEPTED = 'driver_accepted',
  HUBS_ACCEPTED = 'hubs_accepted',
  SCHEDULED = 'scheduled',
  DRIVER_REJECTED = 'driver_rejected',
  PICKED_UP = 'picked_up',
  DELIVERED = 'delivered',
  FRAUDULENT = 'fraudulent',
}
